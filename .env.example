# Backend environment example (backend/.env.example)
DB_HOST=postgres
DB_PORT=5432
DB_USER=sgl_user
DB_PASSWORD=changeme
DB_NAME=sgl_database

# JWT configuration
JWT_SECRET=changeme
JWT_EXPIRES_IN=24h

NODE_ENV=development
